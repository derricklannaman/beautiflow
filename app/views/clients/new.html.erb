<%= link_to "Back to All Clients", stylist_clients_path(@authenticated_user), class: 'btn btn-success' %>
<%= link_to "Back to dashboard", stylist_path(@authenticated_user), class: 'btn btn-warning' %>

<h1>Add new Clients</h1>

<div class="form">
  <%= form_for([@authenticated_user, @client], :html => {:multipart => true}) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>

<% end %>
</div><!-- end form -->











